name: setup-node
description: "Setup PNPM ⚙️ - Cache dependencies ⚡ - Install dependencies 🔧"
runs:
  using: "composite"
  steps:
    - name: Setup PNPM ⚙️
      uses: pnpm/action-setup@v4
      with:
        version: 10
        cache: true

    - name: Install dependencies 🔧
      shell: bash
      run: pnpm install --frozen-lockfile